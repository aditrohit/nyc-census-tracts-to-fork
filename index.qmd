---
title: "Census Tracts"
execute: 
  echo: false
---


```{r}
#| echo: false
#| message: false
#| warning: false
library(tidyverse)

df <- read_csv("data/nyc_tracts.csv")

```


```{r}
# Boxplot of median income by race
ggplot(df, aes(x = race, y = med_income)) +
  geom_boxplot(fill = "lightblue") +
  labs(title = "Median Income by Race",
       x = "Race",
       y = "Median Income (USD)") +
  theme_minimal() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))

```